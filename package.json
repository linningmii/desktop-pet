{
  "name": "desktop-pet",
  "version": "0.3.0-rc.1",
  "description": "A cross-platform desktop pet for Windows tray and macOS menu bar.",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "node --check app/pet.js && cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
